# 发明专利申请书（符合审查要求的极度详尽版）

## 一、 发明名称
**一种基于树模型与深度时序网络残差修正的血站采血量预测方法及系统**

## 二、 技术领域
本发明涉及医疗信息技术、人工智能与大数据处理技术领域，特别涉及一种结合极限梯度提升树（XGBoost）与双向长短期记忆网络（Bi-LSTM），并带有确切网络层级、双重约束机制与动态滚动训练机制的血站采血量智能预测方法及系统。

## 三、 背景技术
血液作为紧缺医疗资源，存在严格有效期。传统方法（如ARIMA、单一LSTM或随机森林）在数据输入结构、模型连接方式上存在缺陷：单一深度学习网络在处理抽象的结构化特征（如星期几、是否开学）时容易引发过拟合，而决策树模型无法有效提取基于时间步长的上下文时序矩阵特征。现有技术中涉及大数据融合的人工智能专利，多停留于概念拼接，缺乏具体的数据矩阵构建维度、确切的网络连接层级、约束计算公式等核心落地技术。

## 四、 发明内容
### 1. 发明目的
本发明针对上述技术壁垒，公开了一种具有精确数据预处理矩阵、明确网络层级结构定义及双重约束训练算法的混合人工智能预测方法。

### 2. 技术方案
本发明提供的一种预测方法，其核心包含以下具体数据处理与模型训练步骤：

#### 步骤S1：多源异构数据的输入与量化矩阵构建
获取血站历史采供血系统数据与外部气象及日历数据，构建基础特征数据框 `DataFrame`。
1. **输入特征设定**：
   - 数值型天气特征：当日平均温度、滚动3日均温（$Temp_{rolling3}$）。
   - 分类型周期特征（One-Hot编码）：星期标识（$D \in [0,6]$），是否周末标识（$W \in \{0,1\}$），月份标识（$M \in [1,12]$）。
   - 业务交叉特征量化：设定“节假日强度”向量 $H \in \{0,1,2,3\}$，设定“学校开学状态”向量 $S \in \{0,1,2\}$。
2. **滞后特征矩阵拼接**：采用 Pandas 滑动窗口计算历史血量滚动和（如 $1d\_sum, 3d\_sum, 7d\_sum$），并与上述特征沿时间轴 $t$ 进行拼接，生成包含 $D_{feat}$ 个特征维度的初始特征矩阵 $X_{base}$。

#### 步骤S2：XGBoost 基线模型的交叉验证与OOF残差提取
使用 $X_{base}$ 训练 XGBoost 回归树以剥离宏观趋势并获取真实残差。
1. **训练与连接逻辑**：采用 K=5 折交叉验证（K-Fold）。将训练集等分为5份，每次使用4份数据训练XGBoost模型，预测剩余1份的基准血量 $\hat{y}_{xgb}$。
2. **基线模型超参数设定**：为防止树模型过度拟合导致残差消失，强制限制最大树深 `max_depth = 4`，学习率 `learning_rate = 0.05`，迭代次数 `n_estimators = 200`，评估指标选用 `mean_squared_error`。
3. **输出数据生成**：计算 OOF 残差 $r_t = y_t - \hat{y}_{xgb, t}$。并在 $1\%$ 至 $99\%$ 分位数区间进行 `np.clip` 截断操作，生成稳定的残差序列 $R = \{r_1, r_2, ..., r_T\}$，作为下一级深度网络的靶向目标。

#### 步骤S3：Bi-LSTM 网络的三维输入张量构造（时间平移对齐机制）
本步骤解决网络级联时的“未来数据泄露”与“对齐错位”核心技术问题。
为预测第 $t$ 天的修正残差 $\hat{r}_t$，构建长度为 $L=14$（回溯步长）的滑动序列窗口。
**关键步骤（平移对齐）**：将输入特征向量中的真实残差向过去平移1个时间步（Shift(1)），形成 `[r_{t-1}, 天气_t, 节假日_t, \hat{y}_{xgb,t}]` 的特征组合。由此，构造出深度模型标准输入三维张量（Tensor）：$X_{seq} \in \mathbb{R}^{N \times L \times F}$。其中，$N$ 为样本数，$L=14$ 为序列步长，$F$ 为特征通道数。

#### 步骤S4：Bi-LSTM 网络层级结构设计与训练
将张量 $X_{seq}$ 输入深度学习网络，网络的明确连接级联结构如下：
1. **输入层**：接收尺寸为 $(14, F)$ 的张量输入。
2. **双向长短期记忆层（第一层）**：包含 64 个隐层单元的 Bi-LSTM，输出全序列信息，即张量形状转为 $(14, 128)$。
3. **批归一化与正则化层**：连接 `BatchNormalization()` 层进行梯度平滑，并连接保留率为 80% 的 `Dropout(0.2)` 层。
4. **双向长短期记忆层（第二层）**：包含 32 个隐层单元的 Bi-LSTM，不返回序列，仅输出最终隐状态向量，形状为 $(64,)$。
5. **全连接层与输出层**：连接带有 ReLU 激活函数的 `Dense(32)` 层及带有 Linear 激活函数的 `Dense(1)` 输出层，输出最终的修正残差预测标量 $\hat{r}_t$。

**模型编译与训练参数**：采用 Adam 优化器（初始学习率 $lr=1e^{-3}$，并附加 `clipnorm=1.0` 的梯度裁剪以防梯度爆炸），损失函数设定为均方误差（MSE）。设 `batch_size=32`，使用 `EarlyStopping` 机制（patience=10）进行最多 120 轮 Epoch 训练。

#### 步骤S5：引入双重约束的损失计算与动态滚动更新
1. **网络训练的双重约束损失项（算法核心机制）**：
   在标准 MSE 损失基础上，深度模型的自定义损失函数中引入了“周期一致性”约束惩罚项：
   $$Loss = \sum_{t=1}^{N} (r_t - \hat{r}_t)^2 + \lambda \cdot \max(0, |\sum_{i=1}^{7} \hat{y}_{t+i} - \hat{Y}_{week}| - \epsilon)$$
   同时，对于血型（$k \in \{A,B,O,AB\}$），依据历史消耗比例配置动态惩罚权重矩阵 $W = [\omega_A, \omega_B, \omega_O, \omega_{AB}]$ 乘入对应血型数据的 MSE 中。
2. **输出自适应融合**：最终预测值 $\hat{y}_{final} = \hat{y}_{xgb} + Clip(\hat{r}_t, -\theta \cdot \hat{y}_{xgb}, \theta \cdot \hat{y}_{xgb})$，其中 $\theta$ 为防爆阈值（通常设定为 0.5）。
3. **自动化滚动更新参数**：
   设置触发机制参数：针对特定紧缺血型计算 MAPE。当连续 3 个时间步的 $MAPE > 8\%$ 时，向调度系统发送重置指令，调用过去 $n = 365 \times 3$ 天的全量数据重置 XGBoost 树节点并重置 Bi-LSTM 网络；在常规状态下，每日仅使用最后一日的数据对 Bi-LSTM 的最后两个 Dense 层进行增量反向传播微调。

## 五、 本发明的有益效果
1. **解决大数据级联专利中模型黑盒化问题**：本专利明确了从 Pandas `DataFrame` 到 Keras `Tensor` 的转化规则，详述了特征平移（Shift(1)）对齐公式，避免了同类专利中含糊其辞的数据泄露错误。
2. **确切的拓扑结构与可复现性**：公开了包含 `Bi-LSTM(64)->BatchNorm->Dropout(0.2)->Bi-LSTM(32)->Dense(32)->Dense(1)` 的完整且具体的网络连接层级，为人工智能技术在医疗领域的落地提供了完全可实施的工程架构。
3. **首创的双重约束算式设计**：将医疗血库管理的“血型占比”与“周度备血量”直接转化为带有 $\lambda$ 与惩罚项的神经网络损失函数计算公式，从梯度更新的根源上约束了深度学习模型的盲目发散。

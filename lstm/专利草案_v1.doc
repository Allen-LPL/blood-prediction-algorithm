# 发明专利申请书

## 一、 发明名称
**一种基于树模型与深度时序网络残差修正的血站采血量预测方法及系统**

## 二、 技术领域
本发明涉及医疗信息技术、人工智能与时间序列预测领域，特别涉及一种结合极限梯度提升树（XGBoost）与双向长短期记忆网络（Bi-LSTM）进行残差自适应修正的血站采血量智能预测方法及系统。

## 三、 背景技术
血液作为一种不可替代的紧缺医疗资源，存在严格的有效期（如红细胞通常为35天）以及严苛的存储条件限制。如果血站采血量预测不准：预测过高会导致血液过期报废，造成资源浪费；预测过低则会导致临床用血短缺，威胁患者生命安全。

当前，针对采血量/用血量预测的方法主要分为两类：
1. **传统统计学方法**（如ARIMA、SARIMA）：只能捕捉线性的周期规律，对于由极端天气、突发性节假日（如春节、清明节、开学季等）引起的非线性突变预测能力极差。
2. **单一机器学习模型**（如单纯的随机森林、XGBoost或单纯的LSTM）：单纯的树模型难以捕捉时间序列中前后日期的时序依赖关系；而单纯的深度学习网络在小样本数据（如某血站只有几年的日度数据）上极易产生过拟合，且无法很好地利用离散的分类特征。

为了解决上述问题，本发明提出了一种“基线预测+残差时序修正”的串联混合架构。

## 四、 发明内容
### 1. 发明目的
本发明的目的在于提供一种高精度、鲁棒性强的采血量预测方法，通过XGBoost捕捉天气、节假日等宏观多维特征的基线趋势，并利用双向LSTM结合时间延迟对齐技术对前置模型的预测残差进行精确修正，从而将最终预测误差率（MAPE）显著降低至5%以内。

### 2. 技术方案
本发明提供的一种基于树模型与深度时序网络残差修正的血站采血量预测方法，具体包含以下步骤：

**步骤S1：多维特征构建与预处理**
- 获取目标血站的历史日度采血量数据及对应的外部数据（气象数据、日历数据）。
- 构建动态滞后特征（Lag、Rolling Sum）、节假日标记（春节、开学季等）以及时间周期特征（星期几、是否周末、月份）。

**步骤S2：基于折外交叉验证（Out-of-Fold, OOF）的基线趋势预测与真实残差提取**
- 为防止基线模型在训练集上过拟合导致残差失真，采用 K-Fold（如5折）交叉验证策略训练极限梯度提升树（XGBoost）模型。
- 通过 OOF 预测方式获取训练集每天的“泛化预测值”，用真实采血量减去泛化预测值，得到逼近真实业务场景的**泛化残差序列**。
- 对训练集残差进行百分位截断（Clipping）去极值处理，剔除极其罕见的异常点噪音。

**步骤S3：基于时间对齐（Time-Lagged Alignment）的残差特征构造**
- 构建用于残差预测的多维序列矩阵。该特征矩阵不仅包含前 $T$ 天的历史残差，还将其进行时间平移（Shift），将第 $t-1$ 天的残差与第 $t$ 天的天气特征、时间特征、XGBoost基准预测值对齐融合。
- 解决传统残差预测中的“未来信息泄漏”及“当日环境特征缺失”的问题。

**步骤S4：双向LSTM（Bi-LSTM）残差学习与修正网络构建**
- 构建包含批归一化（Batch Normalization）与随机失活（Dropout）的双向LSTM网络。
- 将步骤S3构造的时间步为 $L$（如 $L=14$）的多维序列特征输入该网络，网络通过正向和反向计算，学习局部突变时间点前后的残差波动规律，输出第 $t$ 天的预测残差。

**步骤S5：自适应防爆修剪与最终结果融合**
- 获取 XGBoost 在测试日的基准采血量预测值 $\hat{y}_{xgb}$，以及 Bi-LSTM 预测的修正残差值 $\hat{r}$。
- 为防止深度网络在极端未知环境下的“灾难性修正”，对预测残差设置自适应阈值（如限制最大修正幅度不超过基线预测值的50%），得到平滑后的修正残差 $\hat{r}_{clipped}$。
- 输出最终融合预测采血量： $\hat{y}_{final} = \hat{y}_{xgb} + \hat{r}_{clipped}$。

## 五、 本发明的有益效果
与现有技术相比，本发明具有以下显著优势：

1. **破除残差失真问题（首创性引入OOF与特征平移）**：本方法放弃了使用训练集直接拟合残差的传统做法，改用折外（Out-of-Fold）交叉验证生成真实的泛化残差；并通过巧妙的 `Shift` 特征平移机制，让深度网络能够在“不看未来答案”的前提下，结合“昨天的误差”和“今天的天气”预测“今天的修正量”，从根本上解决了残差预测中学不到真实规律的问题。
2. **预测精度极高**：通过真实业务数据集（如北京市血液中心多年数据）验证，相较于单一的先进树模型算法，本发明提出的 `XGBoost + Bi-LSTM` 架构可将预测平均绝对百分比误差（MAPE）从5.5%以上大幅压降至**3.8%左右**。
3. **抗极端干扰能力强（自适应融合）**：本方法加入了基于基线预测值的自适应残差修剪机制（Adaptive Clipping），避免了深度学习模型在未见过的极端特征（如罕见暴雨叠加节假日）下产生灾难性偏移，保障了血库调度系统的绝对安全与稳定。

## 六、 摘要
本发明公开了一种基于树模型与深度时序网络残差修正的血站采血量预测方法。该方法首先通过提取天气、节假日、动态滚动值等多维特征，利用K折交叉验证训练XGBoost模型，获取具备真实分布特性的折外泛化残差；随后，通过时间平移对齐技术，将滞后残差与当日时空特征进行融合，构建序列特征矩阵；最后，将其输入至引入批归一化机制的双向长短期记忆网络（Bi-LSTM）中进行残差规律学习，并通过自适应阈值修剪将基准预测与修正残差进行安全融合。本发明有效解决了传统模型难以同时兼顾全局趋势与局部突变的问题，将预测误差率降至极低水平，为血站科学备血与智能调度提供了可靠的技术支撑。